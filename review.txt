Why Score = 0.00 in VOLATILE Regime
Problem 1: EMA_MACD_TREND weight is 0.0 in VOLATILE
The most active strategy — EMA crossover — is completely silenced in VOLATILE regime. Weight = 0.0 means even if it fires with confidence 1.0, it contributes zero to the score.

Problem 2: VWAP_REVERSION weight is 0.0 in VOLATILE
Same issue. Two of the six strategies are dead in VOLATILE.

Problem 3: Right now the bot is correctly classifying as VOLATILE (ATR spike above 1.5× mean)
But VOLATILE is actually the best time for EMA trend and exhaustion signals on XAUUSD. The zero-weights were too conservative.

Problem 4: logger.log_signal doesn't print per-strategy vote details
You can't see which strategies actually fired — the log only shows the final decision. This makes debugging blind.