I have a training pipeline for XAUUSD M1 LightGBM ensemble. 
I need a Python script that:

1. Downloads the dataset from HuggingFace: https://huggingface.co/datasets/Pcitycrypto/xauusd
   - Use: pip install datasets huggingface_hub
   - Or direct CSV download via hf:// path or requests

2. Renames columns to match my pipeline's REQUIRED_COLUMNS:
   - "datetime" -> "time"
   - "tickvol"  -> "tick_volume"
   - keep: open, high, low, close as-is

3. Cleans the data:
   - Parse "time" as datetime
   - Sort ascending by time
   - Drop rows where open/high/low/close/tick_volume is NaN or 0
   - Remove duplicate timestamps
   - Remove weekends (Saturday, Sunday)
   - Remove rows where close <= 0

4. Saves the cleaned file to:
   train_pipeline/data/xauusd_m1.csv

5. Prints a summary:
   - Total rows saved
   - Date range (first and last timestamp)
   - Label distribution preview using:
       horizon=5, buy_threshold=0.0005, sell_threshold=0.0005
     Show count of -1, 0, +1 labels so I can verify SELL signals exist

The final CSV must have exactly these columns in this order:
time, open, high, low, close, tick_volume

No extra columns. Save as UTF-8 CSV with no index column.